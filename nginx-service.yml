apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: NodePort

  selector:
    app: nginx-web
  ports:
    - protocol: TCP
      port: 80         # Puerto del clúster
      targetPort: 80   # Puerto del contenedor NGINX
      nodePort: 30080  # Puerto externo